import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#FFcc00',
    alignItems: 'center',
    justifyContent: 'center',
  },
  stretch: { fontSize: 20 },
  textCtrlSeries: { fontSize: 40 }
});


